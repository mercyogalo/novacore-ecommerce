# Workspace Instructions

- This workspace contains two independent Next.js apps: `client` and `admin`.
- Keep edits isolated to the relevant app unless a change is intentionally shared.
- Favor reusable components, static mock data, and strongly typed frontend-only implementations.
- Use the global design tokens in each app's `styles/index.css` as the single source of truth for visual customization.
