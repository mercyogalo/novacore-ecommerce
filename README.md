# Skincare Ecommerce

Premium frontend-only ecommerce template for an organic skincare brand, split into two independent Next.js apps:

- `client` for the storefront
- `admin` for the dashboard

Both apps are built to be highly reusable, static-data driven, and easy to connect to a backend later.
